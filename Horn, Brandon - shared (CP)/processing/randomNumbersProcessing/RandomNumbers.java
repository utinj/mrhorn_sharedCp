package randomNumbersProcessing;

import processing.core.PApplet;

public class RandomNumbers extends PApplet
{
    /*
     * This demo shows how to generate random numbers
     * using with Processing. The demo without Processing
     * is in the chapter04 source folder.
     */
    
    public void setup()
    {
//        float r = random(10.0f); // 0.0 <= r < 10.0
//        println(r);
        
//        float r = random(5.0f, 10.0f); // 5.0 <= r < 10.0
//        println(r);
        
//        int r = (int) random(6.0f, 11.0f); // 6 <= r <= 10
//        println(r);
    }
    
    public void draw()
    {
        
    }
}
